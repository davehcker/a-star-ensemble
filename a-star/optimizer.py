def optimize(x, y, models,
             criteria=None):
    """Return an optimized version of models based on the criteria

    Returns:
        models (list): a sorted list of best classifying models.
    """
    pass